# Environment files with secrets
.env

# Local override files (user-specific customizations)
docker-compose.override.yml
prometheus/prometheus.local.yml

# Alert rules (environment-specific, except example)
prometheus/rules/*.rules.yml
!prometheus/rules/example.rules.yml

# Docker volumes (if stored locally)
prometheus_data/
grafana_data/

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Backup files
*.bak
*.backup

# Log files
*.log
logs/
